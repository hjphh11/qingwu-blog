# 音乐素材目录

把真实歌曲放到这里：

- **音频**：`public/music/歌曲.mp3`（如 `song-1.mp3`）
- **封面**：`public/music/covers/封面.svg`（或 png/jpg）

然后在 `src/data/music.ts` 里把每首歌的 `src` / `cover` 改成对应路径、`title` / `artist` / `note` 改成真实信息即可。

当前这几首是占位示例（暂无真实可播音频），用来占住页面与播放器结构。
