# 部署说明 · 清吾 (qingwu.ink)

本项目是 **Astro 静态站**,构建产物在 `dist/`,可放任意静态托管。推荐用 **Vercel** 免费托管并绑定 `qingwu.ink`。

## 1. 推送到 GitHub
```bash
git init
git add -A
git commit -m "init"
git remote add origin https://github.com/<你的用户名>/qingwu-blog.git
git push -u origin main
```

## 2. 导入 Vercel
1. 打开 https://vercel.com 登录,点「Add New → Project」。
2. 选择刚才的 GitHub 仓库 `qingwu-blog` → **Import**。
3. 构建设置(自动识别或手动填):
   - **Framework Preset**: `Astro`(自动)
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. 点 **Deploy**,等待部署完成。

## 3. 绑定自定义域名 qingwu.ink
1. 项目 → **Settings → Domains** → 添加 `qingwu.ink`。
2. 到你的**域名注册商**处,按 Vercel 提示添加 DNS 记录(通常 A 记录指向 `76.76.21.21`,或 CNAME 指向 `cname.vercel-dns.com`)。
3. 等待 DNS 生效,Vercel 会签发 **HTTPS 证书**,`https://qingwu.ink` 即可访问。

## 4. 上线后检查
- 访问 `https://qingwu.ink`,确认首页/文章/音乐/友链/分享/关于都正常。
- 检查 `/sitemap-index.xml`、`/rss.xml`、`/favicon.svg` 可访问。
- 在 Vercel「Analytics」看访问数据;可考虑开启 Vercel Analytics(可选)。

## 5. 注意
- 音乐/友链/分享目前是**占位**内容,上线前把真实歌曲、好友、收藏、简介填好(`src/data/*.ts`)。
- 首页爱弥斯主图 `full.png` 较大(约 3.7MB),如需更快首屏,可后续用工具转成 webp/压缩后替换 `public/images/emis/full.png`。
- `site` 配置在 `astro.config.mjs` 里已是 `https://qingwu.ink`。
