# 音乐馆页面 · 设计说明(方案A:本地音频)

> 参考:`青烟只念梨愁`(XingHuiSama)源码的音乐系统,按「爱弥斯暖色 + Astro」改造。
> 数据:**方案A —— 本地音频文件**。
> 配色:暖色系(奶油白/粉/绯红/琥珀金/暖棕)+ 玻璃卡片 + 圆润造型。

---

## 1. 整体定位
一个「音乐馆」:一个能边听边看歌词的播放器页,同时全站有一个**悬浮迷你播放器**常驻,切页不断音。

## 2. 数据(方案A:本地音频)
歌曲数据放在 `src/data/music.ts`,每首一条:

```ts
export type Song = {
  title: string      // 歌名
  artist: string     // 作者/歌手
  cover: string      // 封面图路径,如 /music/covers/xxx.jpg
  audio: string      // 音频路径,如 /music/xxx.mp3
  note?: string      // 一句短评
  lrc?: string       // 可选 LRC 歌词文本(带时间轴)
}
export const playlist: Song[] = [ /* 你的歌 */ ]
```

- 音频放 `public/music/*.mp3`,封面放 `public/music/covers/`。
- 无后端接口;加载全部在本地,构建时打包进静态站。

## 3. 页面长什么样(布局)

### 3.1 页面骨架
```
┌──────────────────────────────────────────────┐
│  [液态玻璃导航条:头像「清吾」+ 菜单]            │
├──────────────────────────────────────────────┤
│  标题「音乐馆」+ 一句副标                       │
│  ↓ 背景:当前歌曲封面的模糊放大图 + 暖色蒙层       │
│                                                │
│  ┌──────────────┐  ┌────────────────────────┐ │
│  │ 播放控制台     │  │  [歌词] [歌单]  (tab)    │ │
│  │  (左)         │  │  (右)                   │ │
│  │  ·大旋转光碟   │  │  ·歌词:逐行滚动居中高亮  │ │
│  │  ·歌名/作者    │  │  ·歌单:列表+搜索框      │ │
│  │  ·进度条/时间   │  │                        │ │
│  │  ·上/播/下 播放 │  │                        │ │
│  │  ·音量/播放模式 │  │                        │ │
│  └──────────────┘  └────────────────────────┘ │
└──────────────────────────────────────────────┘
```

### 3.2 左面板·播放控制台(暖色玻璃大卡)
- 中央:**大圆形旋转光盘**封面,播放时旋转、暂停时停;碟芯有淡粉光晕。
- 下方:歌名(大字)+ 作者(小字)。
- 控制:进度条(粉色,显示 当前/总长)、**上一首 / 播放暂停 / 下一首**、**音量滑块**、**播放模式**(顺序/单曲/随机)。
- 用爱弥斯粉做按钮主色,玻璃背景 `rgba(255,248,238,.5)` + blur。

### 3.3 右面板·歌词 / 歌单(暖色玻璃大卡)
- 顶部两个 tab:`歌词` / `歌单`。
- **歌词 tab**:白色**渐变遮罩**(上淡下淡),歌词**逐行居中,当前行放大高亮、自动滚到中间**;点击某行跳到对应时间。
- **歌单 tab**:顶部**搜索框**(按歌名/作者搜),下面歌曲列表 = 小封面 + 歌名 + 作者;当前播放的项有粉色高亮 + 播放波纹动画。
- 以「歌/作者 + 一句短评」为每行内容。

### 3.4 顶部标题区
- 「音乐馆」大标题(可用毛笔手写体),下一行小副标(如「在旋律里,找到片刻安静」)。

## 4. 全局悬浮迷你播放器(所有页面)
- 钉在屏幕**右下角**,一个**玻璃胶囊**:`旋转小封面 + 歌名 + 当前歌词 + 播放/暂停 + 下一首`。
- **可拖拽**(framer-motion/motion 的 `drag`),取消惯性。
- **拖到左右两边自动收进边缘**:靠近左/右边缘时吸附并**收成一条小贴边标签**(只露一个小条);拖出来再展开。
- **首页隐藏**:在首页(`/`)透明缩小(参考站如此),其它页显示。
- 风格:暖色玻璃胶囊,主题粉。

## 5. 组件与状态
- **MusicProvider**(全局播放状态,React Context + `<audio>`):
  - 状态:`playlist / currentIndex / currentSong / isPlaying / progress / currentTime / duration / currentLyric / volume / isMuted / playMode`
  - 方法:`togglePlay / nextSong / prevSong / handleSeek / playSong / setVolume / toggleMute / togglePlayMode`
  - 歌词:LRC 解析(带时间轴 → 当前行)。
- **MusicClient**(音乐页主体,left/right 面板)。
- **FloatingPlayer**(悬浮迷你播放器)。
- 做成 **Astro 客户端 island(React + Motion)**,在 `Layout` 里挂 MusicProvider + FloatingPlayer,音乐页挂 MusicClient。

## 6. Astro 多页适配(关键)
- Astro 是**多页**,切页会整页重载,`<audio>` 会停。用 **localStorage 持久化**:每次状态变化存 `currentIndex / isPlaying / currentTime / volume / playMode`;页面加载时读回,若之前在播,恢复并尝试续播(浏览器可能拦截自动播放→保持暂停,等用户点一下再播)。
- 这样跳到别的页,悬浮窗仍显示"当前歌",不会复位。

## 7. 视觉/风格规范
- 主色:爱弥斯粉/绯红;底:奶油白;点缀:琥珀金/暖棕;文字:深暖棕。
- 卡片:玻璃拟态(blur+saturate + 高光描边 + 内阴影)+ 圆角;统一圆润。
- 图标:用允许的图标库(Phosphor / Tabler),主题色。
- 动效降级:`prefers-reduced-motion` 关闭旋转/滚动动画。

## 8. ⚠️ 需要你提供的素材(方案A)
1. **音频文件**(你拥有版权/可公开播放的 mp3)。
2. **封面图**(每首歌一张,建议正方形)。
3. **歌名 / 作者 / 一句短评**(可写成文字给我)。
4. (可选)**LRC 歌词**(带时间轴;不给我就只显示歌名/作者,歌词 tab 提示"暂无歌词")。

---

## 阶段归属
此页面在 **《制作方案.md》阶段 4**。阶段 0–3 的骨架、导航、首页、文章先做;阶段 4 按本设计实现音乐馆 + 悬浮播放器,素材等你提供后填充。
